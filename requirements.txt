# Python dependencies for Fun Snaps project
# 
# Note: extract_assets.py only uses Python standard library modules:
# - os, re, json, urllib.request, urllib.parse, pathlib, html.parser, collections
# 
# No external packages are required.
# 
# If you add new Python scripts that require external packages, list them here.
# Then install with: pip install -r requirements.txt
